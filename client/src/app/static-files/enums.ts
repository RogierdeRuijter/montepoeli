export enum ButtonType {
  PRIMARY,
  SECONDARY
}

export enum ButtonFunction {
  LOGIN
}

export enum Icons {
  CHECK,
  CROSS,
  PLUS
}

export enum IconSize {
  SMALL,
  MEDIUM,
  LARGE
}

export enum Actions {
  ADD,
  ACCEPT
}

export enum Winners {
  WHITE = 'white',
  BLACK = 'black',
  DRAW = 'draw'
}
